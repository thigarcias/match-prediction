# BSD (Bzzoiro Sports Data) API Key — free registration at https://sports.bzzoiro.com/register/
# Required for: get_squad (player names/positions), get_team_form with context="recent" (xG, all competitions)
# Without this key, the MCP still works fully for World Cup 2026 group standings, match stats, H2H history, and team form.
BSD_API_KEY=your_free_api_key_here
